#!/usr/bin/env node
// Placeholder MCP server entrypoint for oh-imean.
// Replace with a real MCP implementation before setting disabled=false in .mcp.json.

console.error("[oh-imean] example MCP server is a placeholder. Implement protocol handlers first.");
process.exit(0);
